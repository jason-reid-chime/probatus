#!/usr/bin/env bash
# Runs on git commit.
# Only typecheck when frontend files are staged — avoids blocking commits for unrelated changes.
if git diff --cached --name-only | grep -q '^frontend/'; then
  cd frontend && npx tsc -b --noEmit
fi
